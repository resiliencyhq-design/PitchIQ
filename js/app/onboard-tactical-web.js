const tacticalLinks={LB:["CB","CM","LW"],RB:["CB","CDM","RW"],CB:["GK","CB","LB","RB","CDM"],GK:["CB"],CM:["CAM","CDM","CB","LW","RW"],CDM:["CB","CM","CAM","RB"],CAM:["ST","CM","CDM","LW","RW"],LW:["LB","CM","CAM","ST"],RW:["RB","CDM","CAM","ST"],ST:["LW","RW","CAM"]};
let tacticalSelected=null;
let tacticalTimer=null;
function tacticalCode(marker){return marker.dataset.position||marker.dataset.pos||"";}
function tacticalCenter(layer,marker){const layerRect=layer.getBoundingClientRect();const markerRect=marker.getBoundingClientRect();return{x:markerRect.left-layerRect.left+markerRect.width/2,y:markerRect.top-layerRect.top+markerRect.height/2};}
function tacticalSvg(layer){let svg=layer.querySelector(".tactical-web-svg");if(!svg){svg=document.createElementNS("http://www.w3.org/2000/svg","svg");svg.classList.add("tactical-web-svg");svg.setAttribute("aria-hidden","true");layer.prepend(svg);}return svg;}
function tacticalTargets(markers,selected,code){const selectedCode=tacticalCode(selected);const selectedSlot=selected.dataset.slot||"";const matches=markers.filter(function(marker){return marker!==selected&&tacticalCode(marker)===code;});if(code==="CB"){if(selectedCode==="CB")return matches;const preferred=(selectedSlot==="rb"||selectedCode==="RB"||selectedCode==="CDM")?"cb2":"cb1";const first=matches.filter(function(marker){return marker.dataset.slot===preferred;}).slice(0,1);if(first.length)return first;return matches.slice(0,1);}return matches.slice(0,1);}
function drawTacticalWeb(selected){const layer=document.querySelector('.onboard-step[data-onboard-step="2"] .position-game-layer');const markers=Array.from(document.querySelectorAll('.onboard-step[data-onboard-step="2"] .position-marker'));if(!layer||!markers.length||!selected)return;const svg=tacticalSvg(layer);const code=tacticalCode(selected);const links=tacticalLinks[code]||[];const start=tacticalCenter(layer,selected);svg.innerHTML="";markers.forEach(function(marker){marker.classList.remove("is-selected","is-linked");});selected.classList.add("is-selected");links.forEach(function(linkCode,index){tacticalTargets(markers,selected,linkCode).forEach(function(linked){linked.classList.add("is-linked");const end=tacticalCenter(layer,linked);const line=document.createElementNS("http://www.w3.org/2000/svg","line");line.classList.add("tactical-web-line");line.style.animationDelay=(index*90)+"ms";line.setAttribute("x1",String(start.x));line.setAttribute("y1",String(start.y));line.setAttribute("x2",String(end.x));line.setAttribute("y2",String(end.y));svg.appendChild(line);});});}
function tacticalRedraw(){if(!tacticalSelected)return;clearTimeout(tacticalTimer);tacticalTimer=setTimeout(function(){drawTacticalWeb(tacticalSelected);},80);}
document.addEventListener("click",function(event){const marker=event.target.closest&&event.target.closest('.onboard-step[data-onboard-step="2"] .position-marker');if(!marker)return;tacticalSelected=marker;setTimeout(function(){drawTacticalWeb(marker);},90);});
window.addEventListener("resize",tacticalRedraw,{passive:true});
