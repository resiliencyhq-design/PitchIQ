const ADAPTIVE_SELECTION_KEY = "pitchiqAdaptiveTrainingSelection";
const ADAPTIVE_HISTORY_KEY = "pitchiqAdaptiveMissionHistory";
const PLAYER_NAME_KEY = "pitchiqPlayerName";
const MAX_HISTORY = 12;
const DRILL_LABELS = Object.freeze({ scanning:"Scanning", vision:"Vision", decision:"Decision", reaction:"Reaction", dual:"Dual Task", position:"Positioning" });
let requestToken = 0;
function readJson(key,fallback){ try{ const value=JSON.parse(localStorage.getItem(key)); return value??fallback; }catch{return fallback;} }
function recentMissionIds(){ return readJson(ADAPTIVE_HISTORY_KEY,[]); }
function playerId(){ return localStorage.getItem(PLAYER_NAME_KEY)||undefined; }
function saveSelection(selection){ try{ sessionStorage.setItem(ADAPTIVE_SELECTION_KEY,JSON.stringify(selection)); const history=recentMissionIds(); localStorage.setItem(ADAPTIVE_HISTORY_KEY,JSON.stringify([...history,selection.mission.id].slice(-MAX_HISTORY))); }catch(error){ console.warn("[PitchIQ adaptive training] Could not persist selection",error); } }
export function fallbackSelection(){ return { mode:"fallback", sourceConstructId:"awareness", mission:{ id:"fallback-scan-first", drillId:"scanning", title:"Scan First", description:"Check your surroundings before the next action." }, reason:"Default scanning mission used because adaptive selection was unavailable." }; }
function waitForTrainingHome(token,attempts=20){ if(token!==requestToken||typeof document==="undefined")return Promise.resolve(null); const screen=document.querySelector("#training.training-reactive"); if(screen)return Promise.resolve(screen); if(attempts<=0)return Promise.resolve(null); return new Promise(resolve=>setTimeout(()=>resolve(waitForTrainingHome(token,attempts-1)),50)); }
export function applyAdaptiveMissionToTrainingScreen(screen,selection){ if(!screen||!selection?.mission)return false; const mission=selection.mission,focus=DRILL_LABELS[mission.drillId]||"Football IQ",topLabel=screen.querySelector(".reactive-top span"),phase=screen.querySelector(".reactive-phase"),cue=screen.querySelector(".reactive-cue"),feedback=screen.querySelector(".reactive-feedback"),start=screen.querySelector('[data-action="start-mission-training"]'); screen.dataset.adaptiveMissionId=mission.id; screen.dataset.adaptiveMode=selection.mode||"balanced_evidence_building"; if(topLabel)topLabel.textContent=`${focus} mission`; if(phase)phase.textContent=selection.mode==="personalised"?"Your priority":"Today's focus"; if(cue)cue.textContent=mission.title.toUpperCase(); if(feedback)feedback.textContent=mission.description; if(start){ start.setAttribute("aria-label",`Enter ${mission.title} live rep`); start.textContent="ENTER LIVE REP →"; } return true; }
export async function selectAdaptiveMission(){ try{ const [{AdaptiveTrainingEngine},{resolveAdaptiveTrainingProfile}]=await Promise.all([import("../../src/training/adaptive-training-engine.js"),import("../../src/coaching/adaptive-training-profile.js")]); const profile=resolveAdaptiveTrainingProfile({playerId:playerId(),recentMissionIds:recentMissionIds()}); return AdaptiveTrainingEngine.selectMission(profile); }catch(error){ console.warn("[PitchIQ adaptive training] Falling back to Scan First",error); return fallbackSelection(); } }
async function activateForTrainingRoute(){ const token=++requestToken,screenPromise=waitForTrainingHome(token),selection=await selectAdaptiveMission(); if(token!==requestToken)return; saveSelection(selection); const screen=await screenPromise; if(token!==requestToken||!screen)return; applyAdaptiveMissionToTrainingScreen(screen,selection); }
if(typeof document!=="undefined")document.addEventListener("click",event=>{ const routeButton=event.target.closest?.('[data-route="training"]'); if(routeButton)activateForTrainingRoute(); },true);
if(typeof window!=="undefined")window.addEventListener("hashchange",()=>{ if(location.hash.toLowerCase()==="#training")activateForTrainingRoute(); });
