import { buildFootballIQProgress } from "../../src/progress/football-iq-progress.js";
const PROFILE_KEY="pitchiq.footballIQProfiles.v1",READINESS_KEY="pitchiq.assessmentReadiness.v1";
function readJson(key,fallback){try{return JSON.parse(localStorage.getItem(key)||"null")??fallback}catch{return fallback}}
const model=()=>buildFootballIQProgress({profiles:readJson(PROFILE_KEY,[]),readiness:readJson(READINESS_KEY,null)});
function formatDate(value){const d=new Date(value);return Number.isNaN(d.getTime())?"Date unavailable":new Intl.DateTimeFormat("en-AU",{day:"numeric",month:"short",year:"numeric"}).format(d)}
function scoreDelta(change){if(change.delta==null)return"—";if(change.delta>0)return`▲ +${change.delta}`;if(change.delta<0)return`▼ ${change.delta}`;return"No change"}
function renderHomeProgress(){const home=document.querySelector("#home, [data-home-screen]");if(!home||home.querySelector("[data-football-iq-progress-card]"))return;const p=model(),r=p.readiness,s=r?.summary||{},card=document.createElement("section");card.className="glass tile fiq-progress-card";card.dataset.footballIqProgressCard="true";card.innerHTML=`<span class="kicker">Football IQ progress</span><h2>${p.confidence.label}</h2><div class="fiq-progress-meter"><span style="width:${Math.round((s.evidenceQuality||0)*100)}%"></span></div><div class="fiq-progress-stats"><span><b>${s.sessions||0}</b> sessions</span><span><b>${s.activeDays||0}</b> active days</span></div><p>${r?.ready?"Your evidence is strong enough for a new formal assessment.":"Complete varied missions to strengthen the evidence behind your next assessment."}</p><button class="primary" data-route="results">VIEW PROGRESS</button>`;(home.querySelector(".home-main, .ux-actions, main")||home).appendChild(card)}
function renderResultsProgress(){const results=document.querySelector("#results.fiq-results");if(!results||results.querySelector("[data-football-iq-history]"))return;const p=model(),section=document.createElement("section"),dimensions=p.changes.map(c=>`<article><span>${c.label}</span><b>${scoreDelta(c)}</b></article>`).join(""),history=p.history.map((profile,index)=>`<li><b>Assessment ${p.history.length-index}</b><span>${formatDate(profile.assessmentDate||profile.generatedAt||profile.assessedAt)}</span><strong>${Number.isFinite(profile?.integratedFIQ?.score)?profile.integratedFIQ.score:"—"}</strong></li>`).join("");section.className="fiq-progress-experience";section.dataset.footballIqHistory="true";section.innerHTML=`<header><span>YOUR DEVELOPMENT</span><h2>${p.previous?"Changes since your last assessment":"Your Football IQ starting point"}</h2><p>${p.previous?"Improvement is shown only from formal assessment evidence.":"Complete another assessment later to unlock comparison insights."}</p></header><div class="fiq-change-grid">${dimensions}</div><div class="fiq-history"><h3>Assessment timeline</h3><ol>${history||"<li><span>No completed assessment history yet.</span></li>"}</ol></div><div class="fiq-reassessment ${p.readiness?.ready?"is-ready":"is-building"}"><span>${p.confidence.label}</span><p>${p.readiness?.message||"Training evidence will show when reassessment is appropriate."}</p>${p.readiness?.ready?'<button class="primary mega" data-route="home">START REASSESSMENT</button>':""}</div>`;const closing=results.querySelector(".fiq-closing");closing?closing.before(section):results.querySelector(".fiq-results-wrap")?.appendChild(section)}
function renderProgress(){renderHomeProgress();renderResultsProgress()}
document.addEventListener("click",e=>{if(e.target.closest?.('[data-route="home"], [data-route="results"]'))setTimeout(renderProgress,60)},true);window.addEventListener("pitchiq:assessment-readiness",()=>{document.querySelector("[data-football-iq-progress-card]")?.remove();renderProgress()});window.addEventListener("load",()=>setTimeout(renderProgress,120));const app=document.getElementById("app");if(app)new MutationObserver(()=>requestAnimationFrame(renderProgress)).observe(app,{childList:true,subtree:false});
