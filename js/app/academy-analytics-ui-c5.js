import { getAcademyAnalytics } from "./academy-analytics-c5.js?v=c5-academy-analytics-20260720";
const STYLE_ID="pitchiq-academy-analytics-c5-css";
if(!document.getElementById(STYLE_ID)){const link=document.createElement("link");link.id=STYLE_ID;link.rel="stylesheet";link.href="css/academy-analytics-c5.css?v=c5-academy-analytics-20260720";document.head.appendChild(link)}
const metric=(label,value,detail)=>`<article class="academy-analytics-stat"><span>${label}</span><strong>${value}</strong><small>${detail}</small></article>`;
const mix=items=>items.length?`<ul class="academy-analytics-mix">${items.map(item=>`<li><span>${item.label}</span><div><i style="width:${item.share}%"></i></div><strong>${item.share}%</strong></li>`).join("")}</ul>`:"<p>Complete training in both worlds to establish a reliable activity mix.</p>";
export function renderAcademyAnalytics(){const root=document.querySelector("[data-academy-profile] .academy-profile-content");if(!root)return false;const data=getAcademyAnalytics();let panel=root.querySelector("[data-academy-analytics]");if(!panel){panel=document.createElement("section");panel.dataset.academyAnalytics="true";panel.className="academy-analytics";root.appendChild(panel)}const signature=JSON.stringify(data);if(panel.dataset.signature===signature)return true;panel.dataset.signature=signature;panel.innerHTML=`<section class="academy-analytics-card"><span>ACADEMY ANALYTICS</span><h2>Development over time</h2><p>A descriptive view of training consistency, recorded performance and evidence quality.</p></section><section class="academy-analytics-grid">${metric("7-DAY SESSIONS",data.periods.last7.sessions,`${data.periods.last7.activeDays} active days`)}${metric("28-DAY AVERAGE",data.periods.last28.averageScore||"—",`${data.periods.last28.sessions} sessions`)}${metric("WORKLOAD",data.workload.label,`${data.workload.sessionsPerWeek} sessions/week`)}${metric("VELOCITY",data.velocity.label,`${data.velocity.delta>=0?"+":""}${data.velocity.delta} points`)}</section><section class="academy-analytics-card"><span>EVIDENCE QUALITY</span><div class="academy-analytics-evidence"><strong>${data.summary.evidenceScore}%</strong><div><i style="width:${data.summary.evidenceScore}%"></i></div><small>${data.summary.evidence} · ${data.summary.totalSessions} total sessions across ${data.summary.totalActiveDays} active days</small></div></section><section class="academy-analytics-card"><span>TRAINING MIX</span><h2>Where evidence is coming from</h2>${mix(data.domainMix)}</section>`;return true}
new MutationObserver(()=>queueMicrotask(renderAcademyAnalytics)).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener("hashchange",renderAcademyAnalytics);window.addEventListener("pageshow",renderAcademyAnalytics);renderAcademyAnalytics();