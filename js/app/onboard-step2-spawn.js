/* STEP 2 stability audit
   Temporarily disabled to verify the core onboarding route without extra observers.
   Visual CSS remains loaded; no runtime DOM mutation is performed here.
*/

console.info('[PitchIQ] Step 2 helper disabled for stability audit');
