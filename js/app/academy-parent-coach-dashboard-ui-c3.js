import { getParentCoachDashboard } from "./academy-parent-coach-dashboard-c3.js?v=c3-parent-coach-dashboard-20260720";

const STYLE_ID="pitchiq-parent-coach-dashboard-c3-css";
if(!document.getElementById(STYLE_ID)){const link=document.createElement("link");link.id=STYLE_ID;link.rel="stylesheet";link.href="css/academy-parent-coach-dashboard-c3.css?v=c3-parent-coach-dashboard-20260720";document.head.appendChild(link)}
const list=items=>items.length?`<ul class="academy-report-list">${items.map(item=>`<li><span>${item.label}</span><strong>${item.score}%</strong></li>`).join("")}</ul>`:"<p>More training evidence is needed.</p>";
export function renderParentCoachDashboard(){const root=document.querySelector("[data-academy-profile] .academy-profile-content");if(!root)return false;const data=getParentCoachDashboard();let panel=root.querySelector("[data-parent-coach-dashboard]");if(!panel){panel=document.createElement("section");panel.dataset.parentCoachDashboard="true";panel.className="academy-report";root.appendChild(panel)}const signature=JSON.stringify(data);if(panel.dataset.signature===signature)return true;panel.dataset.signature=signature;panel.innerHTML=`<section class="academy-report-card"><span>PARENT & COACH VIEW</span><h2>Development overview</h2><p>${data.recommendation}</p></section><section class="academy-report-summary"><article class="academy-report-stat"><span>ACADEMY INDEX</span><strong>${data.player.indexScore||"—"}</strong><small>${data.player.indexLabel} · ${data.player.evidence}</small></article><article class="academy-report-stat"><span>THIS WEEK</span><strong>${data.weekly.sessions}</strong><small>${data.weekly.consistency}% consistency</small></article><article class="academy-report-stat"><span>PLANNED LOAD</span><strong>${data.weekly.plannedMinutes}m</strong><small>${data.weekly.plannedSessions} sessions · ${data.weekly.matchDay}</small></article><article class="academy-report-stat"><span>RECENT TREND</span><strong>${data.trend.label}</strong><small>${data.trend.delta>=0?"+":""}${data.trend.delta} points</small></article></section><section class="academy-report-card"><span>STRENGTHS</span><h2>What the player is building well</h2>${list(data.strengths)}</section><section class="academy-report-card"><span>COACH PRIORITIES</span><h2>Where support should focus next</h2>${list(data.priorities)}</section>${data.nextMilestone?`<section class="academy-report-card"><span>NEXT MILESTONE</span><h2>${data.nextMilestone.label}</h2><p>${data.nextMilestone.progress}% complete · ${data.nextMilestone.detail}</p></section>`:""}`;return true}
new MutationObserver(()=>queueMicrotask(renderParentCoachDashboard)).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener("hashchange",renderParentCoachDashboard);window.addEventListener("pageshow",renderParentCoachDashboard);window.addEventListener("pitchiq:academy-match-day",renderParentCoachDashboard);renderParentCoachDashboard();