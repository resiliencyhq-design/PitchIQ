export const MATCH_INTELLIGENCE_VERSION="1.0.0";
export const MATCH_TAGS=Object.freeze(["scanning","decisionMaking","positioning","supportPlay","defensiveAwareness","communication"]);
const LABELS={scanning:"Scanning",decisionMaking:"Decision making",positioning:"Positioning",supportPlay:"Support play",defensiveAwareness:"Defensive awareness",communication:"Communication"};
function cleanText(value,max=240){return String(value||"").trim().slice(0,max);}
function cleanTags(tags=[]){return [...new Set(tags.filter(tag=>MATCH_TAGS.includes(tag)))];}
export function createMatchRecord(input={}){const minutes=Math.max(0,Math.min(180,Number(input.minutesPlayed)||0));return {version:MATCH_INTELLIGENCE_VERSION,id:input.id||`match-${Date.now()}`,createdAt:input.createdAt||new Date().toISOString(),matchDate:input.matchDate||new Date().toISOString(),opponent:cleanText(input.opponent,80),competition:cleanText(input.competition,80),position:cleanText(input.position,40),minutesPlayed:minutes,coachTags:cleanTags(input.coachTags),playerReflection:{wentWell:cleanText(input.playerReflection?.wentWell),difficult:cleanText(input.playerReflection?.difficult),nextFocus:cleanText(input.playerReflection?.nextFocus)}};}
export function summariseMatch(record={}){const tags=cleanTags(record.coachTags);const strength=tags[0]||null;const priority=tags[1]||tags[0]||"scanning";return {strength:strength?LABELS[strength]:"Evidence building",priority:LABELS[priority],coachSummary:tags.length?`${LABELS[strength]} was observed. Continue building ${LABELS[priority].toLowerCase()} through the next training block.`:"No coach observations were recorded. Use the next match to build structured evidence.",recommendedFocus:priority};}
export function buildMatchEvidence(record={}){const summary=summariseMatch(record);return {id:`evidence-${record.id||Date.now()}`,source:"match",sourceId:record.id||null,observedAt:record.matchDate||record.createdAt||new Date().toISOString(),constructTags:cleanTags(record.coachTags),minutesPlayed:Number(record.minutesPlayed)||0,reflection:record.playerReflection||{},summary,updatesFootballIQ:false,contributesToReadiness:true};}
export function buildReadinessContribution(records=[]){const valid=records.filter(Boolean);const tagged=valid.reduce((sum,item)=>sum+cleanTags(item.coachTags).length,0);return {matchCount:valid.length,observationCount:tagged,quality:valid.length?Math.min(1,tagged/(valid.length*3)):0,eligibleEvidence:valid.map(buildMatchEvidence)};}
