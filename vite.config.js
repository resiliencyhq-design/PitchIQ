import { defineConfig } from "vite";

export default defineConfig({
  // Use relative asset URLs so the same build works on GitHub Pages
  // under /PitchIQ/ and on any future custom domain.
  base: "./"
});
